name: C Vulnerability Scan

on:
  pull_request:
    branches: [main]
    paths:
      - '**/*.c'
      - '**/*.cpp'
      - '**/*.cc'
      - '**/*.cxx'
      - '**/*.h'
      - '**/*.hpp'
      - '**/*.hxx'
  push:
    branches: [main]
    paths:
      - '**/*.c'
      - '**/*.cpp'
      - '**/*.h'
      - '**/*.hpp'
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  vuln-scan:
    name: Vulnerability Scan
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Cache pip dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-vuln-scanner
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libclang-dev

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install torch --index-url https://download.pytorch.org/whl/cpu
          pip install scikit-learn numpy tqdm libclang

      - name: Download model from release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Try to download from latest release, fallback to local if not available
          mkdir -p models
          if gh release download --pattern 'best_model.pt' --dir models 2>/dev/null; then
            echo "Downloaded model from release"
          elif [ -f "output/best_model.pt" ]; then
            echo "Using local model"
            cp output/best_model.pt models/
          else
            echo "::warning::No model found. Skipping scan."
            echo "SKIP_SCAN=true" >> $GITHUB_ENV
          fi

      - name: Run vulnerability scan (changed files only)
        if: env.SKIP_SCAN != 'true' && github.event_name == 'pull_request'
        run: |
          python -m vuln_scanner.cli \
            --only-changed \
            --base-ref origin/${{ github.base_ref }} \
            --output-format sarif \
            --output-file c-vuln-results.sarif \
            --prob-threshold 0.5 \
            --model-dir models \
            --verbose

      - name: Run vulnerability scan (full)
        if: env.SKIP_SCAN != 'true' && github.event_name != 'pull_request'
        run: |
          python -m vuln_scanner.cli \
            --path . \
            --output-format sarif \
            --output-file c-vuln-results.sarif \
            --prob-threshold 0.5 \
            --model-dir models \
            --verbose

      - name: Create empty SARIF if scan was skipped
        if: env.SKIP_SCAN == 'true'
        run: |
          echo '{"version":"2.1.0","$schema":"https://json.schemastore.org/sarif-2.1.0.json","runs":[{"tool":{"driver":{"name":"c-vuln-scanner","version":"1.0.0"}},"results":[]}]}' > c-vuln-results.sarif

      - name: Upload SARIF to GitHub Security
        if: always()
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: c-vuln-results.sarif
          category: c-vulnerability-detection

      - name: Upload scan results artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: vulnerability-scan-results
          path: c-vuln-results.sarif
          if-no-files-found: ignore
          retention-days: 30
